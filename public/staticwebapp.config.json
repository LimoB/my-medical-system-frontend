{
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://medicalappoinment.azurewebsites.net/api/:path*"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*"]
  }
}
